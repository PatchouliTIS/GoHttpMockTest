syntax = "proto2";
package youtu.job;

option go_package = "api/youtu_job";

enum JobStatus {
    INIT = 0;  // unused
    WAIT = 1;
    START = 2; // unused
    RUN = 3;
    STOP = 4;  // unused
    FAIL = 5;
    CANCEL = 6;
    DONE = 7;
}
